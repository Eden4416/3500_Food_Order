<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Order | My Profile</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap" rel="stylesheet">
    
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }
        
        .food-bg {
            background: linear-gradient(135deg, #fff8e1 0%, #fff3e0 100%);
            min-height: 100vh;
            position: relative;
        }
        
        .food-icons {
            position: fixed;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
            top: 0;
            left: 0;
            overflow: hidden;
        }
        
        .food-icons i {
            position: absolute;
            font-size: 2rem;
            color: rgba(255, 87, 34, 0.1);
            animation: float 20s infinite linear;
        }
        
        @keyframes float {
            0% { transform: translateY(0) rotate(0deg); }
            100% { transform: translateY(-1000px) rotate(360deg); }
        }
        
        .card {
            background: white;
            border-radius: 24px;
            box-shadow: 
                0 10px 25px rgba(255, 152, 0, 0.1),
                0 5px 15px rgba(255, 152, 0, 0.05);
            overflow: hidden;
            position: relative;
            transition: all 0.3s ease;
        }
        
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 
                0 15px 35px rgba(255, 152, 0, 0.15),
                0 10px 20px rgba(255, 152, 0, 0.1);
        }
        
        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: linear-gradient(to right, #FF9800, #FF5722);
        }
        
        .gradient-text {
            background: linear-gradient(to right, #FF9800, #FF5722);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .btn-primary {
            background: linear-gradient(to right, #FF9800, #FF5722);
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 12px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(255, 152, 0, 0.2);
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(255, 152, 0, 0.3);
        }
        
        .btn-secondary {
            background: white;
            color: #5D4037;
            border: 2px solid #FF9800;
            padding: 10px 20px;
            border-radius: 12px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .btn-secondary:hover {
            background: #fff8e1;
            transform: translateY(-2px);
        }
        
        .form-input {
            width: 100%;
            padding: 14px 16px;
            border: 2px solid #FFE0B2;
            border-radius: 12px;
            font-size: 1rem;
            color: #5D4037;
            background-color: white;
            transition: all 0.3s ease;
        }
        
        .form-input:focus {
            outline: none;
            border-color: #FF9800;
            box-shadow: 0 0 0 3px rgba(255, 152, 0, 0.1);
        }
        
        .form-label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #5D4037;
            display: flex;
            align-items: center;
        }
        
        .form-group {
            margin-bottom: 24px;
        }
        
        .profile-avatar {
            width: 120px;
            height: 120px;
            background: linear-gradient(135deg, #FF9800, #FF5722);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            color: white;
            margin: 0 auto 30px;
            border: 4px solid white;
            box-shadow: 0 8px 20px rgba(255, 152, 0, 0.3);
        }
        
        <%
            const icons = ['fa-user-circle', 'fa-user-edit', 'fa-id-card', 'fa-address-book', 'fa-map-marker-alt', 'fa-phone-alt', 'fa-envelope', 'fa-user-cog'];
        %>
        
        <% for(let i = 0; i < 15; i++) { %>
            .food-icons i:nth-child(<%= i + 1 %>) {
                left: <%= Math.random() * 100 %>%;
                top: <%= Math.random() * 100 %>%;
                animation-delay: <%= Math.random() * 20 %>s;
                font-size: <%= Math.random() * 2 + 1.5 %>rem;
            }
        <% } %>
    </style>
</head>
<body class="food-bg">
    <div class="food-icons">
        <% for(let i = 0; i < 15; i++) { %>
            <i class="fas <%= icons[Math.floor(Math.random() * icons.length)] %>"></i>
        <% } %>
    </div>
    
    <div class="container mx-auto px-4 py-8 relative z-10">
        <div class="max-w-2xl mx-auto">
            <div class="text-center mb-8">
                <h1 class="text-4xl font-bold gradient-text">My Profile</h1>
                <p class="text-[#8D6E63] mt-2">Manage your personal information and preferences</p>
            </div>
            
            <div class="card p-8">
                <div class="profile-avatar">
                    <i class="fas fa-user-circle"></i>
                </div>
                
                <form action="/profile" method="POST">
                    <div class="form-group">
                        <label class="form-label">
                            <i class="fas fa-user mr-2 text-[#FF9800]"></i> Full Name
                        </label>
                        <input 
                            type="text" 
                            name="name" 
                            value="<%= user.name %>"
                            required 
                            class="form-input"
                            placeholder="Enter your full name"
                        >
                    </div>
                    
                    <div class="form-group">
                        <label class="form-label">
                            <i class="fas fa-phone mr-2 text-[#FF9800]"></i> Phone Number
                        </label>
                        <input 
                            type="tel" 
                            name="phone" 
                            value="<%= user.phone || '' %>"
                            class="form-input"
                            placeholder="Enter your phone number"
                        >
                        <p class="text-sm text-[#8D6E63] mt-2">
                            <i class="fas fa-info-circle mr-1"></i> Used for delivery notifications
                        </p>
                    </div>
                    
                    <div class="form-group">
                        <label class="form-label">
                            <i class="fas fa-map-marker-alt mr-2 text-[#FF9800]"></i> Delivery Address
                        </label>
                        <input 
                            type="text" 
                            name="address" 
                            value="<%= user.address || '' %>"
                            class="form-input"
                            placeholder="Enter your delivery address"
                        >
                        <p class="text-sm text-[#8D6E63] mt-2">
                            <i class="fas fa-info-circle mr-1"></i> Please include apartment number if applicable
                        </p>
                    </div>
                    
                    <div class="flex flex-wrap gap-4 mt-8">
                        <button type="submit" class="btn-primary inline-flex items-center">
                            <i class="fas fa-save mr-2"></i> Save Changes
                        </button>
                        
                        <a href="/" class="btn-secondary inline-flex items-center">
                            <i class="fas fa-times mr-2"></i> Cancel
                        </a>
                    </div>
                </form>
            </div>
            
            <div class="mt-8 card p-6">
                <div class="flex items-start">
                    <div class="w-12 h-12 bg-gradient-to-br from-[#4CAF50] to-[#2E7D32] rounded-lg flex items-center justify-center mr-4 flex-shrink-0">
                        <i class="fas fa-shield-alt text-white text-xl"></i>
                    </div>
                    <div>
                        <h3 class="font-bold text-lg text-[#5D4037] mb-2">Account Security</h3>
                        <p class="text-[#8D6E63]">
                            Your personal information is protected with industry-standard encryption. 
                            We never share your data with third parties without your consent.
                        </p>
                    </div>
                </div>
            </div>
            
            <div class="mt-8 flex flex-wrap gap-4 justify-center">
                <a href="/" class="btn-secondary inline-flex items-center">
                    <i class="fas fa-arrow-left mr-2"></i> Back to Home
                </a>
            </div>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const card = document.querySelector('.card');
            card.style.opacity = '0';
            card.style.transform = 'translateY(20px)';
            
            setTimeout(() => {
                card.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                card.style.opacity = '1';
                card.style.transform = 'translateY(0)';
            }, 200);
            
            const form = document.querySelector('form');
            form.addEventListener('submit', function(e) {
                const nameInput = this.querySelector('input[name="name"]');
                if (!nameInput.value.trim()) {
                    e.preventDefault();
                    alert('Please enter your name');
                    nameInput.focus();
                }
            });
        });
    </script>
</body>
</html>