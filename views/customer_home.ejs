<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Order | Customer Home</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap" rel="stylesheet">
    
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }
        
        .food-bg {
            background: linear-gradient(135deg, #fff8e1 0%, #fff3e0 100%);
            min-height: 100vh;
            position: relative;
        }
        
        .food-icons {
            position: fixed;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
            top: 0;
            left: 0;
            overflow: hidden;
        }
        
        .food-icons i {
            position: absolute;
            font-size: 2rem;
            color: rgba(255, 87, 34, 0.1);
            animation: float 20s infinite linear;
        }
        
        @keyframes float {
            0% { transform: translateY(0) rotate(0deg); }
            100% { transform: translateY(-1000px) rotate(360deg); }
        }
        
        .card {
            background: white;
            border-radius: 24px;
            box-shadow: 
                0 10px 25px rgba(255, 152, 0, 0.1),
                0 5px 15px rgba(255, 152, 0, 0.05);
            overflow: hidden;
            position: relative;
            transition: all 0.3s ease;
        }
        
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 
                0 15px 35px rgba(255, 152, 0, 0.15),
                0 10px 20px rgba(255, 152, 0, 0.1);
        }
        
        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: linear-gradient(to right, #FF9800, #FF5722);
        }
        
        .gradient-text {
            background: linear-gradient(to right, #FF9800, #FF5722);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .btn-primary {
            background: linear-gradient(to right, #FF9800, #FF5722);
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 12px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(255, 152, 0, 0.2);
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(255, 152, 0, 0.3);
        }
        
        .feature-card {
            padding: 30px;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            height: 100%;
            color: #5D4037;
        }
        
        .feature-card i {
            font-size: 2.5rem;
            margin-bottom: 20px;
            background: linear-gradient(to right, #FF9800, #FF5722);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .feature-card h3 {
            font-weight: 700;
            margin-bottom: 15px;
            color: #5D4037;
        }
        
        .feature-card p {
            color: #8D6E63;
            margin-bottom: 20px;
            flex-grow: 1;
        }
        
        .welcome-banner {
            background: linear-gradient(135deg, #FF9800, #FF5722);
            color: white;
            padding: 30px;
            border-radius: 24px;
            margin-bottom: 40px;
            box-shadow: 0 10px 25px rgba(255, 152, 0, 0.3);
        }
        
        .user-avatar {
            width: 80px;
            height: 80px;
            background: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            color: #FF5722;
            margin-bottom: 20px;
        }

        <%
            const icons = ['fa-utensils', 'fa-pizza-slice', 'fa-hamburger', 'fa-burger', 'fa-mug-hot', 'fa-cookie', 'fa-ice-cream', 'fa-bowl-food'];
        %>
        
        <% for(let i = 0; i < 20; i++) { %>
            .food-icons i:nth-child(<%= i + 1 %>) {
                left: <%= Math.random() * 100 %>%;
                top: <%= Math.random() * 100 %>%;
                animation-delay: <%= Math.random() * 20 %>s;
                font-size: <%= Math.random() * 2 + 1.5 %>rem;
            }
        <% } %>
    </style>
</head>
<body class="food-bg">
    <div class="food-icons">
        <% for(let i = 0; i < 20; i++) { %>
            <i class="fas <%= icons[Math.floor(Math.random() * icons.length)] %>"></i>
        <% } %>
    </div>

    <div class="container mx-auto px-4 py-8 relative z-10">
        <div class="welcome-banner">
            <div class="flex flex-col md:flex-row items-center justify-between">
                <div class="flex items-center space-x-4 mb-4 md:mb-0">
                    <div class="user-avatar">
                        <i class="fas fa-user-circle"></i>
                    </div>
                    <div>
                        <h1 class="text-3xl font-bold">Welcome back, <span class="text-white"><%= user.name %></span></h1>
                        <p class="text-white opacity-90">What would you like to order today?</p>
                    </div>
                </div>
                <a href="/logout" class="btn-primary inline-flex items-center">
                    <i class="fas fa-sign-out-alt mr-2"></i> Logout
                </a>
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            <a href="/profile" class="card">
                <div class="feature-card">
                    <i class="fas fa-user-circle"></i>
                    <h3 class="text-xl font-semibold">My Profile</h3>
                    <p>View and manage your personal information, delivery addresses, and preferences</p>
                    <span class="text-sm text-[#8D6E63] mt-2">Click to access →</span>
                </div>
            </a>

            <a href="/restaurants" class="card">
                <div class="feature-card">
                    <i class="fas fa-store"></i>
                    <h3 class="text-xl font-semibold">Restaurants</h3>
                    <p>Browse available restaurants, view menus, and discover new dining options</p>
                    <span class="text-sm text-[#8D6E63] mt-2">Click to explore →</span>
                </div>
            </a>

            <a href="/orders" class="card">
                <div class="feature-card">
                    <i class="fas fa-clipboard-list"></i>
                    <h3 class="text-xl font-semibold">My Orders</h3>
                    <p>Track your current orders, view order history, and manage deliveries</p>
                    <span class="text-sm text-[#8D6E63] mt-2">Click to view →</span>
                </div>
            </a>

            <a href="/orders/paid" class="card">
                <div class="feature-card">
                    <i class="fas fa-receipt"></i>
                    <h3 class="text-xl font-semibold">Paid Orders</h3>
                    <p>Access receipts and invoices for your completed and paid orders</p>
                    <span class="text-sm text-[#8D6E63] mt-2">Click to review →</span>
                </div>
            </a>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const cards = document.querySelectorAll('.card');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                
                setTimeout(() => {
                    card.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, index * 100);
            });

            const featureCards = document.querySelectorAll('.card a');
            featureCards.forEach(card => {
                card.addEventListener('click', function(e) {
                    const clickedCard = this.closest('.card');
                    clickedCard.style.transform = 'scale(0.95)';
                    setTimeout(() => {
                        clickedCard.style.transform = '';
                    }, 150);
                });
            });
        });
    </script>
</body>
</html>